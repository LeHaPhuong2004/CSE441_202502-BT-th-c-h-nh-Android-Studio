package com.example.challenger2;

import android.view.KeyEvent;
import android.widget.TextView;

public interface oneditoraction {
    boolean onEditorAction(TextView textView, int actionId, KeyEvent keyEvent);
}
